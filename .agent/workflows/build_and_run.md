---
description: Build (DMG) or Run the AI Researcher application locally
---

# Run Locally (Development)
To run the application in development mode:
// turbo
1. npm run tauri dev

# Build (Production DMG)
To build the application and generate a DMG installer:
// turbo
1. npm run tauri build
